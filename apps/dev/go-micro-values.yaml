apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata: 
  name: go-micro
  namespace: go-microservice
spec:
  chart:
    spec: 
      version: ">=0.2.0"
    test:
      enable: false
    values: 
      replicaCount: 3